function som (n1=0, n2=0){      //Se os valores não forem passados, os parametros terão valor zero.
   return n1 + n2               //Se não a função retornará NaN(não é um número)
}
console.log(som(51,)) //Funciona assim e no formato abaixo.

/*let res =(som(10, 4))
console.log (res)*/
